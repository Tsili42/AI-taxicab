To do:

//1) calculate heuristic at each node while running astar (taxilist,node.x,node.y) returns min{}
2) from pseudocode to java code (A*)
3) find nearest from client/taxi in graph (PointState method)
4) isFinal (PointState method)
5) check if taxis are graph nodes 

Thursday
Θα μπορούσαμε να κάνουμε κατευθείαν αντικατάσταση των taxi απ' τα πιο κοντινά τους nodes στην λίστα των taxis.
